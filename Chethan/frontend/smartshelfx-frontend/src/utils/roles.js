// Role constants used by the frontend. Keep these values in sync with backend Role enum labels.
const Roles = {
    ADMIN: 'Admin',
    MANAGER: 'Manager',
    VENDOR: 'Vendor'
};

export default Roles;
